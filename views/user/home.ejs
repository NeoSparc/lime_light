<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thelimelight</title>
    <style>
        /* Reset and base styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Poppins', sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f4f4f4;
        }

        /* Top bar */
        .top-bar {
            background-color: #2c3e50;
            color: #ecf0f1;
            padding: 5px 0;
            text-align: center;
        }

        /* Header */
        header {
            background-color: #34495e;
            color: #ecf0f1;
            padding: 1rem 0;
            position: sticky;
            top: 0;
            z-index: 1000;
        }
        .nav-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        .logo {
            width: 150px;
            height: 60px;
            display: flex;
            align-items: center;
        }
        .logo img {
            max-width: 100%;
            max-height: 100%;
            object-fit: contain;
        }
        nav ul {
            display: flex;
            list-style: none;
        }
        nav ul li {
            margin-left: 20px;
        }
        nav ul li a {
            color: #ecf0f1;
            text-decoration: none;
            transition: color 0.3s ease;
        }
        nav ul li a:hover {
            color: #3498db;
        }

        /* Main content */
        main {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 0 20px;
        }

        /* Footer */
        footer {
            background-color: #2c3e50;
            color: #ecf0f1;
            padding: 2rem 0;
        }
        .footer-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
        }
        .footer-section {
            flex: 1;
            margin-right: 2rem;
            margin-bottom: 1rem;
        }
        .footer-section h3 {
            margin-bottom: 1rem;
        }
        .contact-form input,
        .contact-form textarea {
            width: 100%;
            padding: 0.5rem;
            margin-bottom: 1rem;
            border: 1px solid #ecf0f1;
            background-color: #34495e;
            color: #ecf0f1;
        }
        .contact-form button {
            background-color: #3498db;
            color: #ecf0f1;
            border: none;
            padding: 0.5rem 1rem;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .contact-form button:hover {
            background-color: #2980b9;
        }

        /* Responsive design */
        @media (max-width: 768px) {
            .nav-container {
                flex-direction: column;
            }
            nav ul {
                margin-top: 1rem;
            }
            nav ul li {
                margin: 0 10px;
            }
            .footer-section {
                flex: 100%;
                margin-right: 0;
            }
        }
        /* Slider styles */
        .slider {
            height: 70vh;
            position: relative;
            overflow: hidden;
        }
        .slider .list .item {
            position: absolute;
            inset: 0 0 0 0;
            opacity: 0;
            transition: opacity 1s ease-in-out, transform 1s ease-in-out;
            transform: scale(1.1);
        }
        .slider .list .item img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        .slider .list .item::after {
            content: '';
            width: 100%;
            height: 100%;
            position: absolute;
            left: 0;
            bottom: 0;
            background-image: linear-gradient(to top, rgba(0,0,0,0.7) 0%, transparent 100%);
        }
        .slider .list .item .content {
            position: absolute;
            left: 10%;
            top: 50%;
            transform: translateY(-50%);
            width: 80%;
            max-width: 800px;
            z-index: 1;
            text-align: center;
        }
        .slider .list .item .content p,
        .slider .list .item .content h2 {
            color: #fff;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }
        .slider .list .item .content p:nth-child(1) {
            text-transform: uppercase;
            letter-spacing: 5px;
            font-size: 1rem;
            margin-bottom: 1rem;
        }
        .slider .list .item .content h2 {
            font-size: 4rem;
            margin: 0 0 1rem;
            letter-spacing: 2px;
        }
        .slider .list .item .content p:last-child {
            font-size: 1.2rem;
            max-width: 600px;
            margin: 0 auto;
        }
        .slider .list .item.active {
            opacity: 1;
            z-index: 10;
            transform: scale(1);
        }
        .slider .arrows {
            position: absolute;
            top: 50%;
            left: 50px;
            right: 50px;
            transform: translateY(-50%);
            z-index: 100;
            display: flex;
            justify-content: space-between;
        }
        .slider .arrows button {
            background-color: rgba(255,255,255,0.3);
            border: none;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            font-size: 1.5rem;
            color: #fff;
            transition: .5s;
            cursor: pointer;
        }
        .slider .arrows button:hover {
            background-color: rgba(255,255,255,0.5);
        }

        /* Responsive design updates */
        @media (max-width: 768px) {
            .slider {
                height: 50vh;
            }
            .slider .list .item .content h2 {
                font-size: 2.5rem;
            }
            .slider .list .item .content p:last-child {
                font-size: 1rem;
            }
            .slider .arrows {
                left: 20px;
                right: 20px;
            }
        }

        @media (max-width: 480px) {
            .slider {
                height: 40vh;
            }
            .slider .list .item .content h2 {
                font-size: 2rem;
            }
            .slider .list .item .content p:nth-child(1) {
                font-size: 0.8rem;
            }
            .slider .list .item .content p:last-child {
                font-size: 0.9rem;
            }
        }

        /* Article cards styles */
        .articles-container {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }
        .article-card {
            background-color: #fff;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .article-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
        }
        .article-card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }
        .article-card .card-body {
            padding: 1.5rem;
        }
        .article-card .category {
            background-color: #3498db;
            color: #fff;
            padding: 0.3rem 0.8rem;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: bold;
            text-transform: uppercase;
            display: inline-block;
            margin-bottom: 0.8rem;
        }
        .article-card .card-title {
            font-size: 1.4rem;
            margin-bottom: 0.8rem;
            color: #2c3e50;
            font-weight: bold;
        }
        .article-card .author {
            font-size: 0.9rem;
            color: #7f8c8d;
            margin-bottom: 1rem;
        }
        .article-card .btn {
            display: inline-block;
            padding: 0.6rem 1.2rem;
            background-color: #2c3e50;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            font-weight: bold;
        }
        .article-card .btn:hover {
            background-color: #34495e;
        }

        @media (max-width: 1024px) {
            .article-card {
                width: calc(33.33% - 20px);
            }
        }

        @media (max-width: 768px) {
            .article-card {
                width: calc(50% - 20px);
            }
            .slider .list .item .content h2 {
                font-size: 3rem;
            }
        }

        @media (max-width: 480px) {
            .article-card {
                width: 100%;
            }
            .slider .list .item .content h2 {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    
    <%- include('layouts/header.ejs') %>

    <main>
        <div class="slider">
            <div class="list">
                <div class="item active">
                    <img src="https://i.ibb.co/6rtypmw/Contribution-of-Islam-for-the-rise-of-Arabian-Culture.png" alt="Articles">
                    <div class="content">
                        <p>HIGHLIGHTS</p>
                        <h2>ARTICLES</h2>
                        <p>This section provides readers with a captivating journey through the complex realms of religion, culture, Sufism, history, and politics.</p>
                    </div>
                </div>
                <div class="item">
                    <img src="https://c.wallhere.com/photos/e6/ae/digital_art_nature_trees_forest_painting_Japanese_branch_bridge-99425.jpg!d" alt="Art">
                    <div class="content">
                        <p>HIGHLIGHTS</p>
                        <h2>ART</h2>
                        <p>Celebrate diverse forms of artistic endeavor, from interviews with creative minds to visually stunning photo essays and artwork showcases.</p>
                    </div>
                </div>
                <div class="item">
                    <img src="https://i.ibb.co/tKwfKkV/antique-quill-pen-writes-old-fashioned-calligraphy-generated-by-ai-24640-80756.jpg" alt="Literature">
                    <div class="content">
                        <p>HIGHLIGHTS</p>
                        <h2>LITERATURE</h2>
                        <p>Showcase various talented writers with innovative literary works including stories, poems, articles, and travelogues.</p>
                    </div>
                </div>
            </div>
            <div class="arrows">
                <button id="prev">&lt;</button>
                <button id="next">&gt;</button>
            </div>
        </div>

        <div class="articles-container">
            <% for(let i = 0; i < articleList.length; i++) { %>
            <div class="article-card">
                <img src="<%= articleList[i].articleImage %>" alt="Article Image">
                <div class="card-body">
                    <span class="category"><%= articleList[i].Category %></span>
                    <h3 class="card-title"><%= articleList[i].Name %></h3>
                    <p class="author">By <%= articleList[i].Author %></p>
                    <a href="/articleDetails?id=<%= articleList[i]._id%>" class="btn">Read more</a>
                </div>
            </div>
            <%}%>
        </div>
    </main>

    <%- include('layouts/footer.ejs') %>
    <script>
        // Slider functionality
        let items = document.querySelectorAll('.slider .list .item');
        let next = document.getElementById('next');
        let prev = document.getElementById('prev');
        let active = 0;
        
        function loadShow() {
            items[active].classList.add('active');
            for (let i = 0; i < items.length; i++) {
                if (i !== active) {
                    items[i].classList.remove('active');
                }
            }
        }

        function nextSlide() {
            active = (active + 1) % items.length;
            loadShow();
        }

        function prevSlide() {
            active = (active - 1 + items.length) % items.length;
            loadShow();
        }

        next.onclick = nextSlide;
        prev.onclick = prevSlide;

        // Auto-change effect
        setInterval(nextSlide, 5000);  // Change slide every 5 seconds

        loadShow();
    </script>
</body>
</html>